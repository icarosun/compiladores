><>
    float valor
    float notas100
    float notas50
    float notas10
    float notas5
    float notas1
    float aux

    print("Digite o valor que deseja sacar:")

    read(valor)

    if ((valor > 600) or (valor < 10)) {
        print("Desculpe, não temos nota suficiente.")
    } else {
        notas100 = valor # 100
        
        aux = valor % 100

        notas50 = aux # 50

        aux = aux % 50

        notas10 = aux # 10

        aux = aux % 10

        notas5 = aux # 5

        aux = aux % 5

        notas1 = aux        

        print("Para sacar a quantia de: ")
        print(valor)
        print("------")
        
        if(notas100 > 0) {
            print(notas100)
            if (notas100 > 1) { 
                print("notas de 100")
            } else {
                print("nota de 100")
            }
        }

        if(notas50 > 0) {
            print(notas50)
            if (notas50 > 1) { 
                print("notas de 50")
            } else {
                print("nota de 50")
            }
        }

        if(notas10 > 0) {
            print(notas10)
            if (notas10 > 1) { 
                print("notas de 10")
            } else {
                print("nota de 10")
            }
        }

        if(notas5 > 0) {
            print(notas5)
            if (notas5 > 1) { 
                print("notas de 5")
            } else {
                print("nota de 5")
            }
        }

        if(notas1 > 0) {
            print(notas1)
            if (notas1 > 1) { 
                print("notas de 1")
            } else {
                print("nota de 1")
            }
        }
    }
...